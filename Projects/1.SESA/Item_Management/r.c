#include <stdio.h>
int main() {    

    int number1=30, number2=5, sum;

    // calculate the sum
    sum = number1 + number2;      
    
    printf("%d + %d = %d", number1, number2, sum);
    return 0;
}
